let arr = [3, 1, 4, 8, -1];
let arr2 = ['elephant', 'ant', 'duck', 'bat'];
//когато функцията върне положително число/без значение колко 0,1,34324/ разменя местата на а и б.
arr2.sort(); // подрежда ги по азбучен ред, дори и цифри.
arr1.sort((a, b) => a - b); // сортираме цифри по големина.
arr1.sort((a, b) => b - a); // сортираме по обратен ред.
// сравнява ги 2 по 2 в масива. bubble sort, insearchle sort.