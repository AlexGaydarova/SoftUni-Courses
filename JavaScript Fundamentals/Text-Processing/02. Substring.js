function substringWord(string, startIndex, count) {
    let endIndex = startIndex + count;
    //substring(startIndex, endIndex?)
    console.log(string.substring(startIndex, endIndex));
}

substringWord("ASentance", 1, 8);